<?php

class messageModel extends database
{
  public function getConverstaions($uuid){
    //   if($this->Query("SELECT * FROM converation WHERE athlete_id = ? ", [$uuid])){
    //     if($this->rowCount() > 0 ){
    //         $row = $this->fetch();
    //         $dbPassword = $row->password;
    //         $userId = $row->uuid;
    //         $role_id=$row->role_id;
    //         $username=$row->username;
    // //         if(password_verify($password, $dbPassword)){     
    // //             return ['status' => 'ok', 'uuid' =>$userId,'username'=>$username,'role'=>$role_id];

    // //         } else {
    // //             return ['status' => 'passwordNotMatched','data'=>''];
             
    // //         }

    // //     } else {
    // //         return ['status' => 'usernameNotFound','data'=>''];
           
    // //     }

    // // }
    // // else{
    // //     return ['status' => 'usernameNotFound','data'=>''];
    // //     echo "na";
    // }
      
      

  }
    
}